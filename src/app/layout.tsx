import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Git Recovery — Backdate Your Contributions",
  description:
    "Create backdated Git commits on your GitHub repositories securely through a GitHub App. Recover lost contributions and fill your contribution graph.",
  keywords: ["git", "github", "contributions", "backdate", "commits"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="antialiased">
        {children}
      </body>
    </html>
  );
}
